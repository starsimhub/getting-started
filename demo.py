"""
Example Starsim demo script to test that installation worked.

Note that the code also works as a single line, and "sim" is optional:

import starsim as ss; ss.demo()
"""
import starsim as ss
sim = ss.demo()